<div class="lease">
  <div class="tenant">
    <%if !lease.landlord_approved%>
      <h4>Landlord has not accepted the lease.!</h4>
    <%end%>
    <%if !lease.tenant_approved%>
      <h4>Tenant has not accepted!</h4>
    <%end%>
    <%if lease.tenant.photo_file_name.nil?%>
      <%= image_tag 'user.png'%>
    <%else%>
      <%= image_tag tenant.photo.url(:thumb)%>
    <%end%>
    <h4>Name: <%=h lease.tenant.firstname%> <%=h lease.tenant.lastname%></h4>
    <h4>Phone: <%=h lease.tenant.phone_number%></h4>
    <h4>Email: <%=h lease.tenant.email%></h4>
  </div>
  <div class="info">
    <h4>Lease start date: <%=h lease.start_date.to_s(:long) %></h4>
    <h4>Lease end date: <%=h lease.end_date.to_s(:long) %></h4>
    <h4>Monthly rent: <%=h lease.rent %> Due: <%=h lease.rent_due_date.ordinalize%> of month</h4>
    <h4>Auto rent collect: <%=h lease.auto_collect %> </h4>
    <h4>Auto increase: <%=h lease.auto_incriment%> Anual increase: <%=h lease.auto_incriment_pct%></h4>
  </div>
  <div class ="clear"></div>
</div>